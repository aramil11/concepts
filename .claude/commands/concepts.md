Run concepts.py on the current project to extract CS concepts and append them to ~/CONCEPTS.md.

Execute this bash command:
```bash
concepts . --output ~/CONCEPTS.md
```

Then tell the user how many concepts were added and where the file is.
